import { FilterLeaguePipe } from './filter-league.pipe';

describe('FilterLeaguePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLeaguePipe();
    expect(pipe).toBeTruthy();
  });
});
